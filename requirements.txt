# ConfScout Conference Aggregator - Python Dependencies
#
# Required for GitHub Actions workflow and local development
# Install with: pip install -r requirements.txt

# HTTP requests for API and web fetching
requests>=2.28.0

# HTML parsing for web scraping (WikiCFP, etc.)
beautifulsoup4>=4.11.0

# Geocoding for location coordinates
geopy>=2.3.0

# Date parsing for conference dates
python-dateutil>=2.8.0

# (Optional) Discord notifications
# If using Discord webhooks, requests is sufficient
